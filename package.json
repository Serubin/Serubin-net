{
  "version": "1.0.0",
  "private": true,
  "name": "Serubin-net",
  "description": "Solomon Rubin's online portfolio and resume",
  "repository": "https://github.com/Serubin/Serubin-net.git",
  "license": "MIT",
  "scripts": {
    "build": "yarn build:node && yarn build:static",
    "build:static": "jekyll build -d dist/",
    "build:node": "grunt build",
    "dev": "jekyll serve --watch"
  },
  "devDependencies": {
    "grunt": "^1.0.4",
    "grunt-contrib-clean": "^0.7.0",
    "grunt-contrib-concat": "^1.0.0",
    "grunt-contrib-copy": "^0.7.0",
    "grunt-contrib-uglify": "^0.11.1",
    "grunt-contrib-watch": "^1.1.0",
    "grunt-sass": "^2.0.0",
    "gulp-sass": "^3.1.0",
    "node-sass": "^4.12.0"
  }
}
